# Python - Inheritance

This project covers inheritance in Python: base classes, subclasses, super(), isinstance/issubclass, dir(), and class hierarchies applied to geometry (rectangles and squares).
