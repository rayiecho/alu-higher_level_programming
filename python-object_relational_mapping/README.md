# Python - Object-relational mapping

This project covers connecting Python to a MySQL database two ways: directly with MySQLdb (writing raw SQL, parameterized queries, and preventing SQL injection), and through the SQLAlchemy ORM (defining model classes, querying, inserting, updating, and deleting records as Python objects).
