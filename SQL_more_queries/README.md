# SQL - More queries

This project covers MySQL user creation and privilege management (GRANT), table constraints (NOT NULL, UNIQUE, PRIMARY KEY, FOREIGN KEY, DEFAULT), and advanced querying with subqueries, JOIN, LEFT JOIN, and GROUP BY across multiple tables.
