# Python - Network #1

This project covers HTTP requests in Python using both the low-level urllib package and the higher-level requests library: fetching status, reading response headers, sending POST requests, handling HTTP errors, working with JSON APIs, and GitHub API authentication.
