# Python - Almost a circle

This project builds a small object hierarchy (Base -> Rectangle -> Square) covering attribute validation, magic methods, *args/**kwargs, JSON serialization/deserialization, and file persistence, all backed by a full unittest suite.
